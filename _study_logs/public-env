- 两种镜像部署 vllm

- 部署 vllm 服务运行模型的对话 https://deepwiki.com/search/-vllm-python-c-dockerfile_5b96eb2d-2bff-491d-adb0-d1e308b0a7ae?mode=fast


### _方案一_

使用官方镜像 run a container，这样子可以快速部署 vllm 以快速加载 model 开始使用。official image 内部已经编译安装了 vllm

https://deepwiki.com/search/-docker-run-runtime-nvidia-gpu_c013061f-b7f6-4b68-8e78-33ca297bb566?mode=fast

这种方案只能加载一个模型启动一次服务，更换模型需要删除容器重新启动容器。基于官方 image 启动 container 的 sh 脚本，实现一键启动（基于 vllm/docker/run-vllm-model-server-container.sh 脚本）：

- 1. chmod +x run-vllm-model-server-container.sh  （执行一次即可） (`vllm/_study_logs/run-vllm-model-server-container.sh`)

- 2. ./run-vllm-model-server-container.sh Qwen3-4B-Thinking-2507 

    上述命令中有指定 model name



### _方案二_

自行 build 本地 image https://deepwiki.com/search/-v-vllm-cd-homenvidiadesktopvl_cc6083ba-7d29-409d-87f2-eb440062bab3?mode=fast

```shell
# 此过程没有编译或者安装 vllm，单纯的构建一个 pure 开发运行环境
# DOCKER_BUILDKIT=1 的作用是启用 Docker BuildKit 构建引擎，它提供了更高效的镜像构建能力
# https://deepwiki.com/search/-v-vllm-cd-homenvidiadesktopvl_cc6083ba-7d29-409d-87f2-eb440062bab3?mode=fast#16
DOCKER_BUILDKIT=1 docker build . \
    --target dev \
    --tag vllm-dev-gpu \
    --file docker/Dockerfile \
    --build-arg max_jobs=4 \
    --build-arg nvcc_threads=1 \
    --cpuset-cpus="0-3" \
    --memory="8g"

# 2. 并挂载源码启动容器
docker run --runtime nvidia --gpus all \
    -v /home/nvidia/Desktop/vllm:/workspace \
    -v /home/nvidia/Desktop/localmodels:/localmodels \
    -v ~/.cache/huggingface:/root/.cache/huggingface \
    -v ~/.cache/modelscope:/root/.cache/modelscope \
    -p 1128:8000 \
    --ipc=host \
    --name vllm-server-base-builddev-image \
    -it vllm-dev-gpu bash
```

上述命令在创建容器后需要执行 vllm 的安装

- https://deepwiki.com/search/-v-vllm-cd-homenvidiadesktopvl_cc6083ba-7d29-409d-87f2-eb440062bab3?mode=fast#10

- 这里记录了创建容器后的安装工作：https://deepwiki.com/search/-v-vllm-cd-homenvidiadesktopvl_cc6083ba-7d29-409d-87f2-eb440062bab3?mode=fast#13

- 这里记录了推荐的安装方式(说明如何配置增量编译)：https://deepwiki.com/search/-v-vllm-cd-homenvidiadesktopvl_cc6083ba-7d29-409d-87f2-eb440062bab3?mode=fast#14

安装有两个方式：

```shell
# 方式1: 使用预编译(快速)  
VLLM_USE_PRECOMPILED=1 uv pip install -e .  
# 关于使用预编译 c++ 模块部署 vllm 源码服务的补充说明，即执行如下命令：VLLM_USE_PRECOMPILED=1 uv pip install -e . 做了什么工作：
# VLLM_USE_PRECOMPILED=1 表示使用预编译的C++/CUDA扩展,不重新编译 csrc/ 目录。那我请问：
#   那我在执行安装这个 vllm 项目的时候，VLLM_USE_PRECOMPILED=1 uv pip install -e . 是不是下载了相关的预编译文件到指定为止，然后 vllm 源码在部署模型时，会调用这写下载的预编译文件。
#   如果是，下载到了哪里呢 https://deepwiki.com/search/-vllm-24g-docker-run-d-runtime_2a94b3a0-7a9a-4163-bef2-3d648485bc3c?mode=fast#4

# 方式2: 完整编译(学习C++/CUDA)  
uv pip install -e .

# 使用预编译加速
export MAX_JOBS=4  
export NVCC_THREADS=1 
VLLM_USE_PRECOMPILED=1 uv pip install -e .

# 配置增量编译
python tools/generate_cmake_presets.py
# 这样后续修改代码后,只编译变更部分（配置 CMake 增量编译环境）  
cmake --preset release
cmake --build --preset release --target install
```

安装后，启动服务:

```shell
# 如果容器还在运行，直接进入  
docker exec -it vllm-server-base-builddev-image bash  
  
# 如果容器已停止，重新启动并进入  
docker start vllm-server-base-builddev-image  
docker exec -it vllm-server-base-builddev-image bash
cd /workspace  

# model path /localmodels/Qwen3-4B-Thinking-2507
vllm serve /localmodels/Qwen3-4B-Thinking-2507 \
    --gpu-memory-utilization 0.7 \
    --max-model-len 10000 \
    --max-num-seqs 1 \
    --port 8000
```
